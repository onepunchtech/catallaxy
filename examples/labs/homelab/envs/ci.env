# Committed on purpose: these are dummy values for a local or CI lab, so the
# example stands up with no credentials on the machine. A real deployment
# points lab.secrets.envFile somewhere else, or uses a sops store.
CATA_SECRET_BOOTSTRAP__OPENBAO_ROOT_TOKEN__TOKEN=catallaxy-local-openbao-root-token
