# Committed on purpose: a dummy value so the example stands up with no
# credentials on the machine. A real lab points lab.secrets.envFile elsewhere.
CATA_SECRET_BOOTSTRAP__OPENBAO_ROOT_TOKEN__TOKEN=catallaxy-local-openbao-root-token
