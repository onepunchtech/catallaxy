# Well-known phase ordering constants.
# Downstream components reference these instead of hardcoding numbers.
{
  crds = -10;
  namespaces = -5;
  networking = 0;
  operators = 10;
  secrets = 20;
  infrastructure = 30;
  gitops = 40;
  databases = 50;
  apps = 90;
  workloads = 100;
}
