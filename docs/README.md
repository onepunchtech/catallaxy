llms.txt and llms-full.txt are generated into the published site by
`cata-build docs llms` (`cli/src/docs/llms.rs`), from SUMMARY.md and the
page bodies. They are not committed -- fetch them from
https://onepunchtech.github.io/catallaxy/llms.txt or build them with
`nix build .#docs`.
